//
// Created by cv-bhlab on 7/20/21.
//

#include "TrackingStateTransition.h"

namespace HYSLAM{


    TrackingStateTransition::TrackingStateTransition(cv::FileStorage config_data, const std::map<std::string, Camera > &cam_data, optInfo optimizer_info_, std::map< std::string, InitializerData> &init_data_,
                                                       std::ofstream &log, MainThreadsStatus* thread_status_,
                                                     FeatureFactory* factory)
    {

    }
}